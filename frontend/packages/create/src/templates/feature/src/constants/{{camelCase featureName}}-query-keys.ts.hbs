// Copyright 2026 The ThunderID Authors
// SPDX-License-Identifier: Apache-2.0

/**
 * Query key constants for {{camelCase featureName}}s feature cache management.
 */
const {{pascalCase featureName}}QueryKeys = {
  /**
   * Base key for all {{camelCase featureName}}-related queries
   */
  {{constantCase featureName}}S: '{{kebabCase featureName}}s',
  /**
   * Key for a single {{camelCase featureName}} query
   */
  {{constantCase featureName}}: '{{kebabCase featureName}}',
} as const;

export default {{pascalCase featureName}}QueryKeys;
