// Copyright 2026 The ThunderID Authors
// SPDX-License-Identifier: Apache-2.0

import {describe, it, expect} from 'vitest';
import {{pascalCase featureName}}QueryKeys from '../{{camelCase featureName}}-query-keys';

describe('{{pascalCase featureName}}QueryKeys', () => {
  it('should have correct query key values', () => {
    expect({{pascalCase featureName}}QueryKeys.{{constantCase featureName}}S).toBe('{{kebabCase featureName}}s');
    expect({{pascalCase featureName}}QueryKeys.{{constantCase featureName}}).toBe('{{kebabCase featureName}}');
  });

  it('should be readonly', () => {
    // This test ensures the object is properly typed as const
    const keys = {{pascalCase featureName}}QueryKeys;
    expect(typeof keys.{{constantCase featureName}}S).toBe('string');
    expect(typeof keys.{{constantCase featureName}}).toBe('string');
  });

  it('should have unique values', () => {
    const values = Object.values({{pascalCase featureName}}QueryKeys);
    const uniqueValues = new Set(values);
    expect(values.length).toBe(uniqueValues.size);
  });
});
