## ============================================================================
## Sample App Configuration (Development Only)
## ============================================================================
## This file is used for local development. Values are bundled at build time
## and cannot be changed after the app is built.
##
## For distributed/deployed apps, use runtime.json instead.
## ============================================================================

## General App Configuration

# The redirect URI for OAuth flow (where Thunder redirects after authentication)
# E.g., https://localhost:3000
VITE_REACT_APP_REDIRECT_URI=https://localhost:3000

# Enable redirect-based login (OAuth flow)
# Set to 'false' for native flow, 'true' for OAuth redirect flow
VITE_REACT_APP_REDIRECT_BASED_LOGIN=false


## Server Configuration

# Applications endpoint
# E.g., https://localhost:8090/applications
VITE_REACT_APPLICATIONS_ENDPOINT=https://localhost:8090/applications

# Flow endpoint for the server (used for native flow)
# E.g., https://localhost:8090/flow
VITE_REACT_APP_SERVER_FLOW_ENDPOINT=https://localhost:8090/flow

# Application ID registered in Thunder for Basic Authentication
VITE_REACT_APP_AUTH_APP_ID=


## OAuth Configuration (required when VITE_REACT_APP_REDIRECT_BASED_LOGIN=true)

# Server authorization endpoint
# E.g., https://localhost:8090/oauth2/authorize
VITE_REACT_APP_SERVER_AUTHORIZATION_ENDPOINT=https://localhost:8090/oauth2/authorize

# Server token endpoint
# E.g., https://localhost:8090/oauth2/token
VITE_REACT_APP_SERVER_TOKEN_ENDPOINT=https://localhost:8090/oauth2/token

# OAuth authentication endpoint
# E.g., https://localhost:8090/flow/authn
VITE_REACT_AUTHENTICATION_ENDPOINT=https://localhost:8090/flow/authn

# OAuth client ID (from your Thunder application)
VITE_REACT_APP_CLIENT_ID=

# OAuth scopes
VITE_REACT_APP_SCOPE=openid
